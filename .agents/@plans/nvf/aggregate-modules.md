# Subplan: Aggregate modules

## Objective
Ensure the new `nvf.nix` is included in the module aggregation.

## Implementation Steps
1. Update `nix/hjem/default.nix` to include `nvf.nix`.

## Verification
1. Ensure aggregation works correctly.
